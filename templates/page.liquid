<div class="page-width custompage text20 evas-mod cust_padding20">
  <div class="grid">
    <div class="grid__item cust_event">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>
